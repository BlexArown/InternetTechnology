events {}

http {
    server {
        listen 80;

        location /api/sessions {
            proxy_pass http://sessions-svc-s07:8227/sessions;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /health {
            return 200 'gateway ok';
            add_header Content-Type text/plain;
        }
    }
}
